{
  "name": "pairatron",
  "version": "1.0.0",
  "description": "CW pairing app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm run clean && NODE_ENV=production",
    "postinstall": "node heroku-build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.4.1",
    "koa-static": "^4.0.1",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0"
  },
  "devDependencies": {
  }
}
