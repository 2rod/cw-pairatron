{
  "name": "pairatron",
  "version": "1.0.0",
  "engines": {
    "node": "8.6.x"
  },
  "description": "CW pairing app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm run clean && NODE_ENV=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.4.1",
    "koa-static": "^4.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0"
  }
}
